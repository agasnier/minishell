# --- 1. TESTS DE PARSING ET QUOTES ---
echo "hello world"
echo 'hello world'
echo "hello'world"
echo 'hello"world'
echo "hello"world
echo 'hello'world
echo ""
echo " "
echo "   ls   -la   "
echo -n "test" "no" "newline"
echo -nnnnnnn "plusieurs n"
echo -n -n -n "flags separes"
echo "-n" "pas un flag car pas en premier"

# --- 2. GESTION DES VARIABLES D'ENVIRONNEMENT ---
export TEST=42
echo $TEST
echo "$TEST"
echo '$TEST'
export VAR="ls -la"
$VAR
export EMPTY=
env | grep EMPTY
export JOIN=$TEST$USER
echo $JOIN
export "ESPACE=une valeur"
echo "$ESPACE"

# --- 3. REDIRECTIONS (ORDRE ET PRIORITE) ---
echo "texte de base" > file1
cat < file1
echo "ecrasement" > file1
cat < file1
echo "ajout" >> file1
cat < file1
ls > out1 > out2 > out3
cat out3
< file1 cat > file2
cat file2
cat << EOF
ligne 1
ligne 2
$USER
EOF

# --- 4. PIPES ET FLUX ---
ls | sort
ls | grep "file" | wc -l
cat Makefile | grep "CC" | head -n 1
cat /etc/passwd | cut -d: -f1 | sort | uniq | wc -l
ls -la | cat | cat | cat | cat | cat | wc -l

# --- 5. BUILTINS ET DOSSIERS ---
pwd
cd ..
pwd
cd -
mkdir test_dir
cd test_dir
pwd
rmdir ../test_dir
pwd
ls
cd ..

# --- 6. ERREURS ET CODES DE SORTIE ($?) ---
ls non_existent_file
echo $?
export 123INVALID=err
echo $?
expr 1 / 0
echo $?
exit 255
echo "Ce message ne devrait pas s'afficher si exit 255 a fonctionne"

# --- 7. TESTS DE SURVIE (STRESS TEST) ---
          
     ls      -la     
cat < Makefile > out_final
ls | ls | ls | ls | ls | ls | ls
export A=B C=D E=F
unset A C E
env | grep -E "A|C|E"